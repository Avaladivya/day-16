#include<stdio.h>
void main()
{
int p,num,i,j;
printf("enter the number of elements you want");
scanf("%d",&p);
printf("enter the numbers");
i=1;
while(i<=p)
{
scanf("%d",&num);
if(i==1)
j=num;
if(num>j)
j=num;
i++;
}
printf("\n highest %d",j);
}
